{
    "name": "@rhazn/logic-ts",
    "version": "0.1.0",
    "description": "Handle logic in typescript",
    "private": false,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "/dist"
    ],
    "scripts": {
        "build": "tsc",
        "test": "jest --coverage",
        "test:ci": "jest --coverage --ci"
    },
    "dependencies": {
        "typescript": "^4.2.3"
    },
    "devDependencies": {
        "@types/jest": "^26.0.23",
        "ts-jest": "^27.0.1"
    }
}
