{
    "name": "@rhazn/logic-ts",
    "version": "0.5.0",
    "description": "Handle logic in typescript",
    "private": false,
    "repository": {
        "type": "git",
        "url": "git@github.com:rhazn/logic-ts.git"
    },
    "license": "MIT",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "/dist"
    ],
    "scripts": {
        "clean": "rm -Rf dist/",
        "build": "npm run clean & tsc",
        "test": "npm run clean & jest --coverage",
        "test:ci": "npm run clean & jest --coverage --ci"
    },
    "dependencies": {
        "typescript": "^4.2.3"
    },
    "devDependencies": {
        "@types/jest": "^26.0.23",
        "ts-jest": "^27.0.1"
    }
}
